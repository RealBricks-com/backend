package com.app.admin_backend_v1.repository;

import com.app.admin_backend_v1.entity.ProjectCore;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ProjectCoreRepository extends JpaRepository<ProjectCore, Integer> {
}